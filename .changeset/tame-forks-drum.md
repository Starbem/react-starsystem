---
"@starbemtech/react-starsystem": minor
---

Add Drawer/Sheet component (ID-3183) built on @radix-ui/react-dialog, with `left`/`right`/`bottom` positions, `sm`/`md`/`lg`/`full` sizes, optional `title`, `onClose` callback, focus trap, Escape-to-close, and a dark overlay.
