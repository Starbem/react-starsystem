---
"@starbemtech/react-starsystem": minor
---

Add Avatar and AvatarGroup components (ID-3195), with `xs`/`sm`/`md`/`lg`/`xl` sizes, automatic fallback chain (image → initials from `name` → generic icon), an optional `online`/`offline`/`away` `status` indicator, and `AvatarGroup` for a horizontal stack with `+N` overflow. `alt` is required by the type whenever `src` is provided.
