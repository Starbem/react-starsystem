---
"@starbemtech/react-starsystem": minor
---

Add Sidebar and NavItem components (ID-3190), with 1-level nested sub-menus (collapsible), a `collapsed` compact mode (icons only, label shown via `Tooltip` on hover), `header`/`footer` slots, and `aria-current="page"` on the active item.
