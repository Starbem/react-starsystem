---
"@starbemtech/react-starsystem": minor
---

Add Accordion component (ID-3196) built on @radix-ui/react-accordion, with `single`/`multiple` types, `defaultValue`, per-item `disabled`, keyboard navigation, `aria-expanded`/`aria-controls`, and a smooth CSS-driven expand/collapse animation.
