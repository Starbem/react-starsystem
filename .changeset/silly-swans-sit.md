---
"@starbemtech/react-starsystem": minor
---

Add generic Card component (ID-3194), composable via `Card.Header`/`Card.Body`/`Card.Footer`, with `default`/`outlined`/`elevated` variants, `padding` sizes, an accessible clickable mode (`onClick`, keyboard-operable, focus ring), and a `loading` state that swaps content for an internal skeleton.
