---
"@starbemtech/react-starsystem": minor
---

Add TopBar/Header component (ID-3189), with `start`/`center`/`end` slots, `sticky` (with scroll shadow) and `bordered` props, and a responsive layout (title hidden/truncated on narrow screens, actions never shrink).
