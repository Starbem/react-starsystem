---
"@starbemtech/react-starsystem": minor
---

Add Toast/Notification component (ID-3178) built on @radix-ui/react-toast, with a `ToastProvider` + imperative `toast.success/error/warning/info` ToastManager API, configurable position, stacking, auto-dismiss with optional progress bar, and per-variant aria-live.

New runtime dependency: `@radix-ui/react-toast`.
