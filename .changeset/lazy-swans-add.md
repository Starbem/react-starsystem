---
'@starbemtech/react-starsystem': minor
---

Add `Icon` component rendering Material Symbols Outlined, always at weight 200. Adds `material-symbols` as a required peer dependency — consumers must import the font CSS themselves (e.g. `import 'material-symbols/outlined.css'`).
