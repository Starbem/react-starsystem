---
"@starbemtech/react-starsystem": minor
---

Add Breadcrumb component (ID-3191), with `items` (label + href), a customizable `separator`, automatic ellipsis truncation via `maxItems`, and `aria-current="page"` on the last item.
