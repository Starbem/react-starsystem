---
"@starbemtech/react-starsystem": minor
---

Add Tooltip component (ID-3184) built on @radix-ui/react-tooltip, with `top`/`bottom`/`left`/`right` sides, configurable `delay`, a `disabled` escape hatch, and support for rich (non-text) content.

New runtime dependency: `@radix-ui/react-tooltip`.
