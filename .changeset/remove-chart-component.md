---
"@starbemtech/react-starsystem": major
---

Remove the `Chart` component family (`Chart`, `Sparkline`, `LineChart`, `BarChart`, `DonutChart`) and their types (`ChartProps`, `SparklineProps`, `LineChartProps`, `BarChartProps`, `BarDatum`, `DonutChartProps`, `DonutSegment`). Charting is out of scope for this design system — consuming apps should use a dedicated charting library instead.
