---
"@starbemtech/react-starsystem": minor
---

Add Pagination component (ID-3192), with `currentPage`/`totalPages`/`onPageChange`, configurable `siblingCount`, optional `showFirstLast` controls, automatic ellipsis for long page ranges, `aria-current="page"` on the active page, and a compact prev/next + indicator layout for mobile.
