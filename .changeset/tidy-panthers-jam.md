---
"@starbemtech/react-starsystem": minor
---

Add Modal/Dialog component (ID-3182) built on @radix-ui/react-dialog, with `sm`/`md`/`lg`/`xl`/`full` sizes, optional title/description/footer slot, `onClose` callback, `closeOnOverlayClick` toggle, automatic focus trap, Escape-to-close, and internal scroll for overflowing content.

New runtime dependency: `@radix-ui/react-dialog`.
